// Toggle the pre-launch overlay. Set to true to display the Grand Opening launch page.
export const launchMode = false;

// Target launch date: 1 September 2026
export const launchDate = new Date("2026-09-01T00:30:00.000Z");

// WhatsApp business number (international format, no +)
export const whatsappNumber = "917659018774";
export const whatsappMessage =
  "Hi JRG Chicken, Please notify me when ordering starts.";

// Paths that bypass the overlay
export const launchBypassPaths = ["/admin", "/auth", "/reset-password"];
